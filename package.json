{
  "name": "whatseal-mcp",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Sealed WhatsApp MCP — biometric (Touch ID) approval for every externally visible action",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "daemon": "node daemon.mjs",
    "mcp": "node mcp-server.mjs",
    "web": "node web-gateway.mjs",
    "web:gateway": "node web-gateway.mjs",
    "web:ui": "cd web && PORT=3010 BROWSER=none NODE_OPTIONS=--openssl-legacy-provider npm start",
    "web:dev": "WHATSEAL_WEB_DEV_PROXY=http://127.0.0.1:3010 node web-gateway.mjs",
    "web:build": "cd web && NODE_OPTIONS=--openssl-legacy-provider npm run build",
    "web:install": "cd web && npm install",
    "test:e2e": "node --test tests/daemon-lifecycle.e2e.test.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check daemon.mjs && node --check cli.mjs && node --check mcp-server.mjs && node --check web-gateway.mjs && node --check approve-baseline.mjs && node --check lib/lock-power-guard.mjs && node --check lib/browser-lifecycle.mjs && node --check lib/http-policy.mjs && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "express": "^4.22.2",
    "qrcode": "1.5.4",
    "whatsapp-web.js": "git+https://github.com/wwebjs/whatsapp-web.js.git#1780711a1c86dfeca7c5ba6a66f950eac93dde28",
    "zod": "4.4.3"
  }
}
